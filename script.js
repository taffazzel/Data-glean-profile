const menu=document.querySelector('.menu');const nav=document.querySelector('.navigation');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});document.querySelectorAll('.navigation a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const sections=[...document.querySelectorAll('main section[id]')];const links=[...document.querySelectorAll('.navigation a')];const setActive=()=>{let current='home';sections.forEach(s=>{if(scrollY>=s.offsetTop-120)current=s.id});links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+current))};addEventListener('scroll',setActive,{passive:true});setActive();
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.getElementById('year').textContent=new Date().getFullYear();
const canvas=document.getElementById('wave'),ctx=canvas.getContext('2d');let w,h,dpr,points=[];function resize(){dpr=Math.min(devicePixelRatio||1,2);w=canvas.clientWidth;h=canvas.clientHeight;canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0);points=[];for(let r=0;r<24;r++)for(let c=0;c<90;c++)points.push({r,c})}function draw(t){ctx.clearRect(0,0,w,h);const ox=w*.42,oy=h*.52;for(const p of points){const x=ox+(p.c/89)*(w*.72);const z=p.r/23;const wave=Math.sin(p.c*.16+t*.0015+p.r*.34)*18+Math.cos(p.c*.07-p.r*.18+t*.001)*11;const y=oy+(z-.5)*170+wave*(.25+z);const alpha=.08+.62*(1-z)*(.5+.5*Math.sin(p.c*.09+t*.001));const size=.8+2.2*(1-z);ctx.fillStyle=`rgba(13,218,205,${Math.max(.04,alpha)})`;ctx.beginPath();ctx.arc(x,y,size,0,Math.PI*2);ctx.fill();if(p.c<89){const nx=ox+((p.c+1)/89)*(w*.72);const ny=oy+(z-.5)*170+(Math.sin((p.c+1)*.16+t*.0015+p.r*.34)*18+Math.cos((p.c+1)*.07-p.r*.18+t*.001)*11)*(.25+z);ctx.strokeStyle=`rgba(8,172,176,${alpha*.18})`;ctx.lineWidth=.6;ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(nx,ny);ctx.stroke()}}requestAnimationFrame(draw)}addEventListener('resize',resize);resize();requestAnimationFrame(draw);
